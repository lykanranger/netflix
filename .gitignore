# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
# ------------------------------------
# The node_modules folder contains all the packages downloaded by npm. It can be
# huge and should never be committed. It can always be regenerated by running `npm install`.
/node_modules

# Build output
# ------------------------------------
# The /dist folder is the result of the build process. It contains the final,
# optimized files that get deployed. It's generated from your source code.
/dist
/build

# Environment variables
# ------------------------------------
# CRITICAL: This file contains secret keys, API keys, and database credentials.
# Never commit this to a public or private repository.
.env
.env.local
.env.*.local

# Optional: You can commit an example file so others know what variables are needed.
# The '!' negates the rule, so it WILL be committed.
!/.env.example

# Logs
# ------------------------------------
# Log files are generated during runtime and are not part of the source code.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / Editor directories
# ------------------------------------
# These folders store user-specific settings for your code editor.
.vscode/
.idea/

# Operating System generated files
# ------------------------------------
# These are metadata files created by macOS and Windows respectively.
.DS_Store
Thumbs.db